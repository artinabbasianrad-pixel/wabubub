{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r R2Leafy/requirements.txt"
  },
  "deploy": {
    "variables": {
      "SECRET_KEY": {
        "generateValue": true
      }
    },
    "startCommand": "python R2Leafy/main.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
